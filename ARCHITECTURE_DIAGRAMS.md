# 🎨 Visual Architecture Diagrams

## System Overview

```
┌────────────────────────────────────────────────────────────────────┐
│                           USER INTERFACE                            │
│                                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │ EditorCanvas │  │ EditorHeader │  │  EditorBOQ   │             │
│  │              │  │              │  │              │             │
│  │ - Drawing    │  │ - Sync Badge │  │ - BOQ List   │             │
│  │ - Pan/Zoom   │  │ - Actions    │  │ - Export     │             │
│  └──────┬───────┘  └──────┬───────┘  └──────────────┘             │
│         │                  │                                        │
│         │                  │                                        │
└─────────┼──────────────────┼────────────────────────────────────────┘
          │                  │
          ▼                  ▼
┌────────────────────────────────────────────────────────────────────┐
│                       REACT STATE LAYER                             │
│                                                                      │
│  ┌──────────────────────────────────────────────────────┐          │
│  │           useDetections Hook                          │          │
│  │                                                        │          │
│  │  State: [detections, syncStatus]                     │          │
│  │  Actions: add(), update(), remove(), refresh()       │          │
│  └──────────────────────────┬───────────────────────────┘          │
│                              │                                       │
└──────────────────────────────┼───────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│                    DETECTION SYNC SERVICE                           │
│                                                                      │
│  ┌─────────────────────┐           ┌──────────────────┐           │
│  │   IndexedDB Store   │◄─────────►│   Sync Queue     │           │
│  │                     │           │                  │           │
│  │  - detections       │           │  - pendingOps    │           │
│  │  - syncStatus       │           │  - retryCount    │           │
│  │  - timestamps       │           │  - timers        │           │
│  └─────────────────────┘           └──────────────────┘           │
│           ▲                                 │                       │
│           │                                 │                       │
│           │         ┌──────────────────────▼────────┐              │
│           └─────────┤  Debounce + Retry Logic      │              │
│                     │  (1s delay, 3 retries)        │              │
│                     └──────────────────────┬────────┘              │
│                                            │                        │
└────────────────────────────────────────────┼────────────────────────┘
                                             │
                                             ▼
┌────────────────────────────────────────────────────────────────────┐
│                         NETWORK LAYER                               │
│                                                                      │
│  ┌────────────────────────────────────────────────────┐            │
│  │  API Endpoints (api.js)                            │            │
│  │                                                      │            │
│  │  GET    /detections/pages/{id}    (fetch)          │            │
│  │  POST   /detections/pages/{id}    (create)         │            │
│  │  PUT    /detections/{id}          (update)         │            │
│  │  DELETE /detections/{id}          (delete)         │            │
│  │  POST   /detections/batch-sync    (batch)          │            │
│  └────────────────────────────────────────────────────┘            │
│                              │                                       │
└──────────────────────────────┼───────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│                      BACKEND API LAYER                              │
│                                                                      │
│  ┌────────────────────────────────────────────────────┐            │
│  │  FastAPI Routes (detections.py)                    │            │
│  │                                                      │            │
│  │  @router.get("/pages/{page_id}")                   │            │
│  │  @router.post("/pages/{page_id}")                  │            │
│  │  @router.put("/{detection_id}")                    │            │
│  │  @router.delete("/{detection_id}")                 │            │
│  │  @router.post("/batch-sync")                       │            │
│  └──────────────────────┬─────────────────────────────┘            │
│                         │                                           │
└─────────────────────────┼───────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                             │
│                                                                      │
│  ┌────────────────────────────────────────────────────┐            │
│  │  DetectionService (detection_service.py)           │            │
│  │                                                      │            │
│  │  - get_detections_by_page()                        │            │
│  │  - create_detection()                               │            │
│  │  - update_detection()                               │            │
│  │  - delete_detection()                               │            │
│  │  - batch_sync()                                     │            │
│  └──────────────────────┬─────────────────────────────┘            │
│                         │                                           │
└─────────────────────────┼───────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────────────────┐
│                      DATABASE LAYER                                 │
│                                                                      │
│  ┌────────────────────────────────────────────────────┐            │
│  │  PostgreSQL Database                                │            │
│  │                                                      │            │
│  │  Table: detections                                  │            │
│  │  - id (UUID)                                        │            │
│  │  - project_id                                       │            │
│  │  - page_id                                          │            │
│  │  - bbox_x1, bbox_y1, bbox_x2, bbox_y2             │            │
│  │  - class_name, confidence                           │            │
│  │  - is_manual, created_at                            │            │
│  └────────────────────────────────────────────────────┘            │
│                                                                      │
└────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: User Draws a Box

```
Step 1: USER ACTION (0ms)
┌─────────────────────┐
│   User completes    │
│   drawing gesture   │
└──────────┬──────────┘
           │
           ▼
Step 2: OPTIMISTIC UPDATE (< 16ms)
┌─────────────────────────────────────────────────────────┐
│  EditorCanvas.handleMouseUp()                           │
│    ↓                                                     │
│  onAddDetection({ bbox_x1, bbox_y1, ... })             │
│    ↓                                                     │
│  useDetections.add()                                    │
│    ↓                                                     │
│  detectionSyncService.addDetection()                    │
│    ├── Generate temp ID: "temp_1738091234567_abc123"   │
│    ├── Save to IndexedDB                                │
│    ├── Set syncStatus = "pending"                       │
│    └── Update React state (UI re-renders)              │
└─────────────────────────────────────────────────────────┘
           │
           │
           ▼
Step 3: USER SEES BOX (< 16ms total) ✅ INSTANT!
┌─────────────────────┐
│   Canvas re-renders │
│   Box appears       │
└─────────────────────┘
           │
           │ (1 second debounce)
           │
           ▼
Step 4: BACKGROUND SYNC (after 1s)
┌─────────────────────────────────────────────────────────┐
│  detectionSyncService.syncDetection()                   │
│    ↓                                                     │
│  syncCreateToBackend()                                  │
│    ↓                                                     │
│  POST /api/detections/pages/{pageId}                    │
│    ↓                                                     │
│  DetectionService.create_detection()                    │
│    ↓                                                     │
│  INSERT INTO detections                                 │
│    ↓                                                     │
│  Server responds with real ID: "uuid-1234-5678"        │
│    ↓                                                     │
│  Replace temp detection with server version             │
│    ↓                                                     │
│  Set syncStatus = "synced"                              │
└─────────────────────────────────────────────────────────┘
           │
           ▼
Step 5: SYNC COMPLETE
┌─────────────────────┐
│  Badge shows: ✓     │
│  "Synced"           │
└─────────────────────┘
```

---

## Sync State Machine

```
┌──────────────┐
│   Created    │
│  (new box)   │
└──────┬───────┘
       │
       ▼
┌──────────────────┐      Saved to IndexedDB
│   syncStatus:    │◄─────────────────────────
│    "pending"     │
└──────┬───────────┘
       │
       │ Debounce timer (1s)
       │
       ▼
┌──────────────────┐
│   Sync attempt   │
└──────┬───────────┘
       │
       ├─────────────────┐
       │                 │
       ▼                 ▼
  SUCCESS           NETWORK ERROR
       │                 │
       │                 ├──► Retry 1 (2s delay)
       │                 │      ├──► Retry 2 (4s delay)
       │                 │      │      ├──► Retry 3 (8s delay)
       │                 │      │      │      │
       │                 │      │      │      ▼
       │                 └──────┴──────┴──► FAILED
       │                                     │
       ▼                                     ▼
┌──────────────────┐              ┌──────────────────┐
│   syncStatus:    │              │   syncStatus:    │
│    "synced"      │              │     "error"      │
│                  │              │                  │
│  Badge: ✓ Green  │              │  Badge: ! Red    │
└──────────────────┘              └──────────────────┘
```

---

## IndexedDB Structure

```
Database: InTakeOffDB
└── Object Store: detections
    ├── Index: page_id (for filtering)
    ├── Index: syncStatus (for querying pending)
    └── Index: updatedAt (for ordering)

Example Entry:
{
  id: "temp_1738091234567_abc123",      // Temporary ID
  project_id: "proj-uuid-here",
  page_id: "page-uuid-here",
  bbox_x1: 100.5,
  bbox_y1: 200.3,
  bbox_x2: 150.8,
  bbox_y2: 250.9,
  class_name: "Manual_Item",
  confidence: 1.0,
  is_manual: true,
  syncStatus: "pending",                // ← Sync state
  createdAt: 1738091234567,
  updatedAt: 1738091234567,
  retryCount: 0
}

After Sync:
{
  id: "real-uuid-from-server",          // ← Real ID
  ...same fields...
  syncStatus: "synced",                 // ← Updated
  updatedAt: 1738091236789,
  retryCount: 0
}
```

---

## Component Communication

```
┌─────────────────────────────────────────────────────────────┐
│                    ProjectEditor.jsx                         │
│  ┌──────────────────────────────────────────────┐           │
│  │  const { detections, add, remove, syncStatus } │           │
│  │        = useDetections(pageId)                │           │
│  └──────────────┬─────────────────┬──────────────┘           │
│                 │                 │                           │
│        ┌────────▼─────┐   ┌──────▼──────┐                   │
│        │EditorCanvas  │   │EditorHeader │                   │
│        │              │   │             │                   │
│        │onAddDetection│   │SyncBadge    │                   │
│        └──────────────┘   └─────────────┘                   │
└─────────────────────────────────────────────────────────────┘
         │                          │
         │                          │
         ▼                          ▼
    add(boxData)            syncStatus { syncing, pendingCount }
         │                          │
         ▼                          ▼
┌─────────────────────────────────────────────────────────────┐
│              detectionSyncService                            │
│                                                               │
│  addDetection() ──┬──► IndexedDB.put()                      │
│                   └──► scheduleSyncDetection()               │
│                                                               │
│  subscribe() ─────────► notify all listeners                 │
└─────────────────────────────────────────────────────────────┘
```

---

## Offline/Online Transitions

```
ONLINE MODE
┌─────────────────────────────────────────────┐
│  User draws box                             │
│    ↓                                         │
│  IndexedDB save (instant)                   │
│    ↓                                         │
│  1s debounce                                 │
│    ↓                                         │
│  API call succeeds                           │
│    ↓                                         │
│  syncStatus = "synced" ✓                    │
└─────────────────────────────────────────────┘

                    │
                    │ User goes offline
                    ▼

OFFLINE MODE
┌─────────────────────────────────────────────┐
│  User draws 5 boxes                         │
│    ↓                                         │
│  All saved to IndexedDB instantly           │
│    ↓                                         │
│  1s debounce → API calls fail               │
│    ↓                                         │
│  syncStatus = "pending" (5 pending) ⏱       │
│    ↓                                         │
│  Badge shows: "5 pending"                   │
└─────────────────────────────────────────────┘

                    │
                    │ Connection restored
                    ▼

RECONNECTION
┌─────────────────────────────────────────────┐
│  Sync service detects connection            │
│    ↓                                         │
│  Auto-retry all pending operations          │
│    ↓                                         │
│  API calls succeed                           │
│    ↓                                         │
│  Replace temp IDs with real IDs             │
│    ↓                                         │
│  syncStatus = "synced" ✓                    │
│    ↓                                         │
│  Badge shows: "Synced"                      │
└─────────────────────────────────────────────┘
```

---

## Performance Timeline

```
Traditional (Blocking):
┌──────────┬──────────┬──────────┬──────────┬──────────┐
│   Draw   │   API    │  Wait    │  Response│  Update  │
│  Gesture │  Call    │  200ms   │  Received│    UI    │
└──────────┴──────────┴──────────┴──────────┴──────────┘
0ms       10ms       210ms                    410ms

Total: 410ms ❌ SLOW


Optimistic (Our Implementation):
┌──────────┬──────────┬──────────────────────────────────┐
│   Draw   │  Update  │       Background Sync            │
│  Gesture │    UI    │      (happens silently)          │
└──────────┴──────────┴──────────────────────────────────┘
0ms       10ms  15ms  1s (debounce)        1.2s (complete)

Perceived: 15ms ✅ INSTANT!
Actual sync: 1.2s (user doesn't wait)
```

---

## Browser Storage Usage

```
┌────────────────────────────────────────────────────┐
│              Browser Storage Layers                 │
├────────────────────────────────────────────────────┤
│                                                     │
│  1. React State (In-Memory)                        │
│     ├── Fastest access (< 1ms)                     │
│     ├── Cleared on page refresh                    │
│     └── Used for: Current detections array         │
│                                                     │
│  2. IndexedDB (Persistent)                         │
│     ├── Fast access (< 10ms)                       │
│     ├── Survives page refresh                      │
│     ├── ~50MB+ quota                                │
│     └── Used for: All detection data + sync state  │
│                                                     │
│  3. Server Database (PostgreSQL)                   │
│     ├── Slower (200ms+)                             │
│     ├── Permanent storage                           │
│     ├── Unlimited size                              │
│     └── Used for: Source of truth                  │
│                                                     │
└────────────────────────────────────────────────────┘

Sync Strategy:
React State ──(instant)──► User sees changes
     │
     └──(auto)──► IndexedDB ──(debounced)──► Server
                    │
                    └──(on refresh)──► React State
```

---

## Error Recovery Flow

```
┌─────────────────────────────────────────────────────────┐
│  Operation: Add Detection                               │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
           IndexedDB Save ✓
                   │
                   ├──────────────────┐
                   │                  │
                   ▼                  ▼
           Network OK          Network Error
                   │                  │
                   ▼                  │
           Sync Success               │
                   │                  │
                   ▼                  ▼
           Status: "synced"    Retry #1 (2s delay)
                                      │
                                      ├──────────────┐
                                      │              │
                                      ▼              ▼
                                  Success       Still Failing
                                      │              │
                                      ▼              ▼
                              Status: "synced"  Retry #2 (4s)
                                                     │
                                                     ├──────────┐
                                                     │          │
                                                     ▼          ▼
                                                Success    Still Failing
                                                     │          │
                                                     ▼          ▼
                                             Status: "synced"  Retry #3 (8s)
                                                                │
                                                                ├────────┐
                                                                │        │
                                                                ▼        ▼
                                                           Success   Failed
                                                                │        │
                                                                ▼        ▼
                                                        Status: "synced" Status: "error"
                                                                         │
                                                                         ▼
                                                                  Show red badge
                                                                  User can retry
```

---

This visual guide helps understand the complete system architecture!
